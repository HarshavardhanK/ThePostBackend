doctype html
html
    head
        meta(charset='UTF-8')
        title Article #{_id}
        link(rel='stylesheet', href='/css/styles.css')
        link(href='https://fonts.googleapis.com/css?family=Lobster|Ubuntu:300&display=swap',rel='stylesheet')
        link(href='https://fonts.googleapis.com/icon?family=Material+Icons',rel='stylesheet')

    body

        -function prettyDate(dateString){

          -var date = new Date(dateString);
          -var d = date.getDate();
          -var monthNames = [ "January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December" ];
          -var m = monthNames[date.getMonth()];
          -var y = date.getFullYear();
          -return d+' '+m+', '+y;
        -}

        div.title !{title}

        img(src=featured_media).featured
        span.author #{author.name}
        span.date #{prettyDate(date)}

        span.content !{content}
        span.share-webview
          span.share-webview-inner
            a.share-link-footer(href='/posts/render/' + _id + '/share')
              i.material-icons.share-icon share
              span &nbsp&nbspShare This Article
